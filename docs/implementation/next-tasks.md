# 下一自动任务

1. 完成 ASP.NET Core Identity/Profile 垂直切片、Migration、单元和 PostgreSQL 集成测试。
2. 完成 Next.js 登录与首次资料流程。
3. 完成 Expo 登录与首次资料流程，Token 持久化使用 SecureStore 适配。
4. 建立 OpenAPI 生成客户端和漂移检查。
5. 创建 GitHub Actions，生成 lockfile 并执行 lint、typecheck、test、build、Migration 和契约检查。
6. 根据真实 CI 失败逐项修复，直到当前切片全部通过。
